## Summary

<!-- One or two sentences: what does this PR do? -->

## Motivation

<!-- Why is this change needed? Link issues with `Fixes #123`. -->

## Changes

<!-- Bullet list of notable changes. -->

## Type of change

- [ ] Bug fix (non-breaking change which fixes an issue)
- [ ] New feature (non-breaking change which adds functionality)
- [ ] Breaking change (fix or feature that would cause existing functionality to not work as expected)
- [ ] Documentation update
- [ ] Refactor (no functional change)
- [ ] Test improvement

## Checklist

- [ ] `pnpm run build` passes
- [ ] `pnpm test` passes
- [ ] Dashboard builds (`cd dashboard && pnpm run build`) if dashboard touched
- [ ] Tests added for new behavior
- [ ] Documentation updated (`README.md`, `docs/`, `CHANGELOG.md`) if user-visible change
- [ ] No secrets, credentials, or personal data in diff
- [ ] No `any` types — used `unknown` + type guards
- [ ] Commit messages follow Conventional Commits (`feat:`, `fix:`, `docs:`, etc.)

## Test plan

<!-- How did you verify this change? Include commands, URLs, or screenshots. -->

## Screenshots / output

<!-- If UI change or new output format, attach before/after. -->

## Security impact

<!-- If this touches auth, secrets, or network boundaries, explain the security implications. "None" is a valid answer. -->
